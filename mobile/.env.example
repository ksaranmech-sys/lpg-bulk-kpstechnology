# Optional. Overrides where the app sends API requests.
# Leave unset for the defaults: your PC's local backend while developing in Expo Go,
# and the hosted server (app.json -> extra.apiBaseUrl) in production builds.
# EXPO_PUBLIC_API_BASE_URL=http://192.168.1.10:5001/api/v1
